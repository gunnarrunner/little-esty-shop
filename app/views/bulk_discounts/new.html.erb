<%= form_with model: [@merchant, BulkDiscount.new], local: true do |f| %>
  <%= f.label :percentage_discount, "Percentage Discount" %>
  <p> <%= f.number_field :percentage_discount, step: "0.01" %> </p>

  <%= f.label :quantity_threshold, "Quantity Threshold" %>
  <p> <%= f.number_field :quantity_threshold %> </p>

  <%= f.submit "Create Bulk Discount" %>
<% end %>